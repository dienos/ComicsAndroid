<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>
        <import type="android.view.View" />
        <variable
            name="item"
            type="com.kstd.android.jth.domain.model.local.BookmarkItem" />
        <variable
            name="viewModel"
            type="com.kstd.android.jth.ui.feature.home.ComicsViewModel" />
    </data>

    <androidx.cardview.widget.CardView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="8dp"
        android:onClick="@{() -> viewModel.onBookmarkItemClick(item)}"
        android:onLongClick="@{() -> viewModel.onBookmarkItemLongClick(item)}"
        app:cardCornerRadius="8dp">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="140dp"
            android:orientation="horizontal">

            <ImageView
                android:id="@+id/iv_thumbnail"
                android:layout_width="100dp"
                android:layout_height="match_parent"
                android:scaleType="centerCrop"
                app:imageUrl="@{item.thumbnail}"
                app:imageWidth="@{item.sizeWidth}"
                app:imageHeight="@{item.sizeHeight}"
                tools:srcCompat="@tools:sample/backgrounds/scenic" />

            <TextView
                android:id="@+id/tv_title"
                android:layout_width="0dp"
                android:layout_height="match_parent"
                android:layout_weight="1"
                android:layout_marginStart="16dp"
                android:layout_marginEnd="8dp"
                android:gravity="center_vertical"
                android:maxLines="3"
                android:ellipsize="end"
                android:text="@{item.title}"
                tools:text="Bookmark Title Goes Here" />

            <CheckBox
                android:id="@+id/cb_bookmark_select"
                android:layout_width="48dp"
                android:layout_height="wrap_content"
                android:layout_gravity="center_vertical"
                android:layout_marginEnd="8dp"
                android:onClick="@{() -> viewModel.onBookmarkItemClick(item)}"
                android:checked="@{item.isSelected}"
                android:visibility="@{item.isSelectionMode ? View.VISIBLE : View.GONE}" />

        </LinearLayout>

    </androidx.cardview.widget.CardView>

</layout>
